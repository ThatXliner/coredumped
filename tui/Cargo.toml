[package]
name = "coredumped-tui"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "coredumped"
path = "src/main.rs"

[dependencies]
coredumped-core = { path = "../core" }
bracket-color = "~0.8"
crossterm = "0.25"
clap = { version = "4", features = ["derive"] }
log = "0.4"
